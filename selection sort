def selection_sort(arr):
    n = len(arr)
    for i in range(n):
        min= i
        for j in range(i+1, n):
            if arr[j] < arr[min]:
                min_ = j
        arr[i], arr[min]= arr[min],arr[i]

arr = [64, 25, 12, 22, 11]
selection_sort(arr)
print("Sorted array is:", arr)
